# 项目规则

## 代码修改前必须走计划流程

任何写代码、改代码、删文件的操作之前，必须按以下流程走，不可跳过：

### 第 1 步：上下文分析
- 读取项目相关文件（CLAUDE.md、README、配置文件、需求涉及的源码）
- 全面总结当前状态：项目结构、技术栈、核心模块、相关代码现状、已有问题

### 第 2 步：问题复述
- 用你自己的话复述我的需求，确认理解正确
- 有歧义立刻追问，不要猜

### 第 3 步：提出方案
- 编号列出行动计划（改什么、创建什么、删除什么）
- 代码修改：说明涉及的文件、函数/类、修改理由
- 新功能：说明架构、文件结构、关键设计决策
- 标出风险和权衡

### 第 4 步：请求确认
- 结尾必须明确询问："是否按此方案执行？（是/否/修改）"
- 我没说"是"之前，不准动手

### 第 5 步：执行
- 严格按批准的方案执行
- 遇到意外问题暂停汇报，不准擅自变通

---

## 防幻觉规则（最高优先级）

- 任何关于已有代码的说法，必须引用你实际读过的文件路径和行号或函数名
- 不准凭空编造函数名、类名、变量名、文件路径、API 签名、配置键、依赖版本
- 不准根据名字猜函数的行为——必须先读函数体
- 没读过的文件不准描述内容，直接说"我尚未读取此文件"
- 只改你实际读过的代码。波及未读文件时标记"需验证"
- 严格区分：
  - ✅ "在 `utils.py:42`，`load_config()` 从 `.env` 读取——我实际读到的"
  - ❌ "这个项目应该用 FastAPI 加 SQLAlchemy"（除非真读过）
  - ⚠️ "⚠️ 假设：API 遵循 REST 规范——尚未验证路由定义"
- 必须做假设时显式标注 "⚠️ 假设："，并说明需读什么来验证
- 执行中发现假设与事实冲突，立即停下汇报
