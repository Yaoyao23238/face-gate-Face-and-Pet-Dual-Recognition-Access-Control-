# 人脸识别门禁系统 → 人脸+宠物双模门禁 升级方案

## 一、项目背景

本项目是一个基于 FaceNet 的实时人脸识别门禁系统：

- **检测**：MTCNN 三级级联人脸检测
- **特征提取**：InceptionResnetV1（VGGFace2 预训练）→ 512 维 embedding
- **匹配**：余弦相似度 + 自动阈值（mean − 2σ）
- **运行**：纯 CPU 实时推理，OpenCV 摄像头捕获，Tkinter 非阻塞动画
- **准确率**：125 样本测试，整体 90.40%

目标：新增宠物品种识别能力，使门禁系统在无人脸时自动切换到宠物检测，识别为注册品种后放行。

## 二、技术方案

### 2.1 模型选型：YOLOv8m

| 对比维度 | 选择理由 |
|----------|----------|
| 参数量 | 25.9M，GPU 推理 ~8ms（RTX 3060），CPU 亦可降级运行 |
| 精度 | Oxford-IIIT Pet 37 品种，fine-tune 后预期 Top-1 > 93% |
| 一体化 | 单一模型同时完成动物检测框 + 品种分类，无需两阶段串联 |
| 生态 | ultralytics 库一键训练/导出/推理，代码量极少 |
| 数据匹配 | 37 品种 × ~200 张/品种 ≈ 7400 张，YOLOv8m 容量刚好不欠拟合也不过拟合 |

排除项：
- YOLOv8n/s：精度略低，GPU 条件下不值得省那几毫秒
- YOLOv8l/x：数据量不足以支撑，容易过拟合
- RT-DETR：优势在小目标/密集场景，门禁场景单只宠物近距大目标，用不上 Transformer 全局注意力
- 两阶段方案（检测器 + 分类器）：多一次推理开销，无精度增益

### 2.2 任务定义

**品种级分类，非个体识别。** 同一品种的不同个体视为同一身份，全部放行。

```
品种白名单示例：{"英短", "柯基", "橘猫", "金毛", "布偶猫", "柴犬"}
```

推理流程：

```
摄像头帧 → YOLOv8m 推理 → 输出 (检测框, 品种标签, 置信度)
  → 置信度 > 0.7 且品种在白名单 → 触发开门动画 + 日志
  → 否则 → 显示拒绝
```

### 2.3 与现有人脸系统共存

双分支平级，人脸优先：

```
摄像头帧 →
  分支 A（现有）: MTCNN → FaceNet → 人脸余弦匹配 → 通过/拒绝
  分支 B（新增）: YOLOv8m → 品种标签 → 白名单匹配 → 通过/拒绝

合并策略：有人脸走 A，无人脸走 B，两者都有走 A
```

A 和 B 共享：动画系统、日志系统、冷却机制（20s）、帧驱动非阻塞架构。

## 三、训练方案

### 3.1 数据集

- **来源**：Oxford-IIIT Pet Dataset
  - 37 个猫狗品种（12 猫 + 25 狗）
  - 每品种约 200 张，总计 ~7400 张
  - 自带品种标签 + 边界框 + 像素级分割标注
- **下载方式**：
  ```bash
  # 方式一：ultralytics 自动下载
  from ultralytics import YOLO
  # 方式二：手动下载 + 格式转换脚本
  ```

### 3.2 数据划分

```
data/raw/                     # 原始下载
    → 转换脚本 →
data/train/images/ + labels/  # 70%，~5180 张
data/val/images/   + labels/  # 15%，~1110 张
data/test/images/  + labels/  # 15%，~1110 张
```

### 3.3 训练配置

```yaml
# pet_gate.yaml
path: pet_recognition/data
train: train/images
val: val/images
test: test/images

nc: 37
names:
  [Abyssinian, Bengal, Birman, Bombay, British_Shorthair, Egyptian_Mau,
   Maine_Coon, Persian, Ragdoll, Russian_Blue, Siamese, Sphynx,
   American_Bulldog, American_Pit_Bull_Terrier, Basset_Hound, Beagle,
   Boxer, Chihuahua, Cocker_Spaniel, Dachshund, Dobermann,
   English_Cocker_Spaniel, English_Setter, German_Shorthaired,
   Great_Pyrenees, Havanese, Japanese_Chin, Keeshond, Leonberger,
   Miniature_Pinscher, Newfoundland, Pomeranian, Pug, Saint_Bernard,
   Samoyed, Scottish_Terrier, Shiba_Inu]
```

训练参数：

```python
from ultralytics import YOLO

model = YOLO("yolov8m.pt")  # 预训练权重

model.train(
    data="pet_gate.yaml",
    epochs=50,
    imgsz=640,
    batch=16,
    lr0=0.001,
    lrf=0.01,
    optimizer="AdamW",
    cos_lr=True,
    freeze=10,              # 冻结 backbone 前 10 层
    augment=True,           # Mosaic + HSV + 翻转（默认）
    device=0,               # GPU
    project="pet_recognition/logs",
    name="yolov8m_pet_gate",
    exist_ok=True
)
```

预期耗时：~2 小时（单 GPU RTX 3060）

### 3.4 模型导出

```python
model.export(format="pt")           # PyTorch 原始
model.export(format="onnx")         # ONNX（可选，CPU 加速）
```

权重存放：`pet_recognition/models/pet_gate.pt`

## 四、工程集成

### 4.1 新增依赖

```bash
pip install ultralytics
```

### 4.2 配置文件 `pet_recognition/config/pet_config.json`

```json
{
  "model_path": "pet_recognition/models/pet_gate.pt",
  "confidence_threshold": 0.7,
  "allowlist": ["英短", "柯基", "橘猫", "金毛", "布偶猫", "柴犬"],
  "detect_interval": 10,
  "cooldown_seconds": 20,
  "input_size": 640
}
```

### 4.3 核心代码片段

`pet_recognition/scripts/pet_detector.py`：

```python
from ultralytics import YOLO
import cv2
import json

class PetDetector:
    def __init__(self, config_path):
        with open(config_path, "r", encoding="utf-8") as f:
            cfg = json.load(f)
        self.model = YOLO(cfg["model_path"])
        self.threshold = cfg["confidence_threshold"]
        self.allowlist = set(cfg["allowlist"])
        self.input_size = cfg["input_size"]

    def detect(self, frame):
        """
        返回: (breed_name, confidence) 或 None
        """
        results = self.model(frame, imgsz=self.input_size, verbose=False)
        if len(results[0].boxes) == 0:
            return None

        # 取置信度最高的检测框
        best_idx = results[0].boxes.conf.argmax()
        cls_id = int(results[0].boxes.cls[best_idx])
        conf = float(results[0].boxes.conf[best_idx])

        breed_name = self.model.names[cls_id]

        if conf > self.threshold and breed_name in self.allowlist:
            return (breed_name, conf)
        return None
```

### 4.4 集成到主检测循环

`detect_fixed.py` 改动点（伪代码）：

```python
from pet_recognition.scripts.pet_detector import PetDetector

pet_detector = PetDetector("pet_recognition/config/pet_config.json")

# 在主循环中
while True:
    ret, frame = cap.read()
    if not ret:
        break

    # 分支 A：人脸检测（现有逻辑，不改）
    if frame_count % 5 == 0:
        faces = mtcnn.detect(frame)
        if faces:
            # ... 现有人脸识别逻辑 ...
            continue  # 有人脸时不走宠物分支

    # 分支 B：宠物检测（新增）
    if frame_count % pet_interval == 0:
        result = pet_detector.detect(frame)
        if result:
            breed, conf = result
            trigger_open_animation(f"宠物·{breed}")
            log_access(f"[宠物] 品种：{breed} | 置信度：{conf:.3f} | 通过")
            time.sleep(cooldown)
        else:
            # 无人脸也无已知宠物 → 保持待机
            pass

    frame_count += 1
```

### 4.5 日志格式扩展

`access_log.txt` 新增行格式：

```
[2026-06-07 14:32:18] [宠物] 品种：英短 | 置信度：0.923 | 通过
[2026-06-07 14:35:01] [宠物] 品种：未知 | 拒绝
```

## 五、评估方案

### 5.1 测试指标

| 指标 | 计算方式 | 目标 |
|------|----------|------|
| 品种分类 Top-1 | 测试集准确率 | > 93% |
| 检测召回率 | 宠物有无被漏检 | > 98% |
| 白名单通过率 | 白名单品种被正确放行 | > 95% |
| 拒绝准确率 | 非白名单品种被正确拒绝 | > 90% |
| CPU 推理延迟 | 单帧推理耗时 | < 100ms |
| GPU 推理延迟 | 单帧推理耗时 | < 15ms |

### 5.2 评估脚本

`pet_recognition/scripts/evaluate.py`：

```python
from ultralytics import YOLO
import json

model = YOLO("pet_recognition/models/pet_gate.pt")
results = model.val(
    data="pet_gate.yaml",
    split="test",
    device=0
)
print(f"mAP50:    {results.box.map50:.3f}")
print(f"mAP50-95: {results.box.map:.3f}")
print(f"Top-1:    {results.box.top1:.3f}")
```

## 六、文件清单

### 新增文件

```
pet_recognition/
├── config/
│   └── pet_config.json          # 白名单 + 阈值配置
├── data/
│   ├── raw/                     # Oxford-IIIT Pet 原始数据（下载后）
│   ├── train/images/ + labels/  # 训练集（转换后）
│   ├── val/images/   + labels/  # 验证集
│   └── test/images/  + labels/  # 测试集
├── models/
│   └── pet_gate.pt              # 训练好的 YOLOv8m 权重
├── scripts/
│   ├── download_dataset.py      # 数据集下载 + 格式转换
│   ├── train.py                 # 训练脚本
│   ├── pet_detector.py          # 推理封装类
│   └── evaluate.py              # 评测脚本
├── logs/                        # 训练日志
└── pet_gate.yaml                # YOLO 数据集配置
```

### 修改现有文件

| 文件 | 改动量 | 说明 |
|------|--------|------|
| `detect_fixed.py` | +15 行 | 新增宠物检测分支，人脸优先 |
| `config.json` | +5 行 | 新增 `pet_enabled`、`pet_interval` 字段 |
| `animation.py` | 0 行 | 完全复用 |
| `access_log.txt` | 自动追加 | 日志格式自动兼容 |

## 七、执行步骤

按顺序执行：

### Phase 1：数据与训练（Day 1 上午）

```
[ ] 1. pip install ultralytics
[ ] 2. 运行 download_dataset.py → 下载 Oxford-IIIT Pet 到 data/raw/
[ ] 3. 运行格式转换 → 生成 YOLO 格式标注到 data/train/val/test/
[ ] 4. 运行 train.py → GPU 训练 ~2 小时
[ ] 5. 运行 evaluate.py → 验证 mAP 和 Top-1
```

### Phase 2：集成（Day 1 下午）

```
[ ] 6. 编写 pet_detector.py 推理封装类
[ ] 7. 修改 detect_fixed.py，加入宠物检测分支
[ ] 8. 本地摄像头测试（用宠物照片/视频模拟）
[ ] 9. 调优置信度阈值 → 平衡通过率和误判率
```

### Phase 3：测试（Day 1 傍晚）

```
[ ] 10. 真实宠物测试（如条件允许）
[ ] 11. 白天/夜晚不同光照测试
[ ] 12. 宠物遮挡/侧脸/远景边界测试
[ ] 13. 人脸+宠物同框优先级测试
```

**总工期：1 天。**

## 八、注意事项与风险

| 风险 | 缓解 |
|------|------|
| Oxford-IIIT Pet 品种名是英文，需映射中文 | config 中维护 `en→zh` 映射表 |
| 同品种不同毛色（如橘猫 vs 白猫同属 Persian） | 无需区分，品种级放行，符合预期 |
| 摄像头角度低才能拍到宠物 | 可能需要调整摄像头位置或增加第二摄像头 |
| CPU 模式双模型负载 | YOLOv8m 比 FaceNet 轻，影响可控；可设 pet_interval=10 降低检测频率 |
| 非宠物类别（如玩偶、图片）被误检 | 置信度阈值 0.7 + 白名单双重过滤 |
| 动物不配合（不面朝摄像头） | 训练时使用多角度数据增强，检测间隔内总能捕获到正脸 |

## 九、后续扩展方向

1. **活体检测**：分析连续帧的眨眼/耳朵抖动微动，防止照片攻击
2. **多宠物同框**：当前取最高置信度单只，可改为同时检测并依次判定
3. **移动端部署**：YOLOv8n + NCNN/TNN 量化 → 树莓派/手机端推理
4. **品种无关的个体识别**：回到度量学习方案，区分同品种不同个体
5. **行为分析**：检测到宠物后联动录像保存、推送通知
